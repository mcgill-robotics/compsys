#!/bin/bash

# Connecting to robot
alias robot='mosh robotics@10.0.0.1'
alias robotx='ssh -Y robotics@10.0.0.1'

# Bagging
alias bag='python -m bag.run'

# Monitoring
alias glances='glances -yet 5'
alias main='tmux attach -t MAIN || tmux new -s MAIN'
